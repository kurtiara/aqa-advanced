let radius = 32;
let height = 111;
// const PI = Math.PI
let radius_height = Math.PI * Math.pow(radius, 2) * height;
let rounded_radius_height = radius_height.toFixed(2);
console.log(rounded_radius_height);
